require('nightfox').setup({
  fox = "nordfox",
  styles = {
    comments = 'italic',
    strings = 'italic',
    variables = 'bold',
    functions = 'bold',
    keywords = 'bold',
  },
})

require('nightfox').load()
